name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions: {}

env:
  CLOAKED_REQ_BUILD: "true"

jobs:
  test:
    if: github.event_name == 'push' || github.event.pull_request.user.login == github.repository_owner
    name: OTP ${{ matrix.otp }} / Elixir ${{ matrix.elixir }}
    runs-on: ubuntu-latest
    permissions:
      contents: read # check out the repository
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}-test-${{ matrix.otp }}-${{ matrix.elixir }}
      cancel-in-progress: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - otp: "28"
            elixir: "1.19"
            version-type: "loose"
          - otp: "29.0"
            elixir: "1.20.0-rc.6"
            version-type: "strict"

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install zizmor
        shell: bash
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL https://github.com/zizmorcore/zizmor/releases/download/v1.25.2/zizmor-x86_64-unknown-linux-gnu.tar.gz \
            | tar -xz -C "$HOME/.local/bin" zizmor
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake golang-go

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable # zizmor: ignore[superfluous-actions] toolchain action provides target/component management not in the runner image
        with:
          toolchain: stable

      - name: Cache Rust
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: native/cloaked_req_native/target
          key: rust-${{ hashFiles('native/cloaked_req_native/Cargo.lock') }}
          restore-keys: rust-

      - name: Rust tests
        run: cargo test --manifest-path native/cloaked_req_native/Cargo.toml

      - uses: erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1.24.0
        env:
          INSTALL_DIR_FOR_OTP: ${{ runner.tool_cache }}/setup-beam/otp-${{ matrix.otp }}
          INSTALL_DIR_FOR_ELIXIR: ${{ runner.tool_cache }}/setup-beam/elixir-${{ matrix.elixir }}
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}
          version-type: ${{ matrix.version-type }}

      - name: Cache Elixir
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            _build
            deps
            ~/.mix
          key: mix-otp${{ matrix.otp }}-elixir${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}-${{ hashFiles('native/cloaked_req_native/Cargo.lock') }}
          restore-keys: mix-otp${{ matrix.otp }}-elixir${{ matrix.elixir }}-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile
        run: MIX_ENV=test mix compile

      - name: Check formatting
        run: MIX_ENV=test mix format --check-formatted --dry-run

      - name: Credo
        run: MIX_ENV=test mix credo --strict

      - name: Dialyzer
        run: MIX_ENV=test mix dialyzer

      - name: Elixir tests
        run: MIX_ENV=test mix test --color

      - run: zizmor --offline --persona=pedantic .github/workflows
